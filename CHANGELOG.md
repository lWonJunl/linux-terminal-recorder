# 패치 노트

버전은 `MAJOR.MINOR.PATCH` 형식을 따른다. GitHub 업로드 전 기록은 날짜만 표시하고, 이후 버전은 한국 표준시(KST)를 함께 기록한다.

## v1.3.3 — 2026-09-22 23:02:55 KST

- `man`, `vi`, `less`, `top` 등의 전체 화면 전환 후 원래 스크롤 기록이 돌아오면 기록을 계속하도록 수정했다.
- 실제 기록 삭제, 버퍼 초과, 창 크기 변경처럼 원래 기록이 돌아오지 않는 경우는 계속 차단한다.
- 이전 버전에서 전체 화면 전환으로 중단된 세션도 정상 백업과 현재 기록이 이어지면 복구할 수 있도록 했다.
- 전체 화면 전환과 실제 기록 손실을 구분하는 회귀 테스트를 추가했다.

## v1.3.2 — 2026-09-21 08:40:33 KST

- 캡처 임시 파일이 충돌할 때 기존 파일을 삭제할 수 있던 오류를 수정했다.
- 이번 실행에서 직접 만든 임시 파일만 실패 시 정리하도록 변경했다.

## v1.3.1 — 2026-09-20

- 상태 파일의 형식과 필수 항목을 검증하도록 보완했다.
- 기록 폴더 밖으로 벗어나는 경로와 링크·정션을 차단했다.
- 기록 손상 후 같은 백업이 반복 생성되는 문제를 수정했다.

## v1.3.0 — 2026-09-18

- 녹화용 Bash에서 `clear`, `reset`, `Ctrl+L`을 제한하는 기록 보호 기능을 추가했다.
- 최종 PNG/PDF와 캡처 조각·백업·상태 파일의 저장 위치를 분리했다.

## v1.2.0 — 2026-09-18

- 명령어마다 화면을 저장하던 방식에서 스크롤백 전체를 한 번에 캡처하는 방식으로 변경했다.
- 처음부터 끝까지 이어지는 파노라마 PNG 한 장과 PDF 한 페이지를 생성하도록 구현했다.

## v1.1.0 — 2026-09-17

- 전체 화면 대신 실제 WSL 터미널 창만 캡처하도록 개선했다.
- `stop`이 기록 프로세스를 종료한 뒤 PNG/PDF를 생성하도록 저장 순서를 안정화했다.
- 저장 실패 시 원본 보존과 복구가 가능하도록 보완했다.

## v1.0.0 — 2026-09-17

- `start`, `stop`, `status`, `recover` 명령을 갖춘 첫 실행형 기록기를 완성했다.
- 화면 캡처, 즉시 원본 저장, PNG/PDF 생성의 기본 흐름을 구현했다.

## 초기 개발 — 2026-09-17

- Python 기반 구현을 시작하고 WSL 실행, 화면 캡처, 이미지 연결, PDF 생성을 개발 범위로 정했다.

Windows Terminal 프로필 선택 기능은 외형을 시험하기 위한 실험이었으므로 버전 계산에서 제외했다. 문구와 README만 정리한 변경도 기능 버전을 올리지 않는다.
