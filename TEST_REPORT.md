# 검증 결과

자동 테스트·공개 준비·프로필 지정 점검일: 2026-09-21. Windows Python 3.14.2 기준.
실제 WSL 화면·보안 도구 점검일은 아래에 별도로 기록한다.

## 수정 및 회귀 검사

다음 보호 동작을 구현하고 회귀 테스트로 검증한다.

| 항목 | 반영 내용 |
| --- | --- |
| 세션 경로 신뢰 문제 | 시작·상태·종료·복구에서 정규화한 경로와 기록 폴더 경계를 확인한다. 기존 링크·정션이 외부를 가리키는 경우도 거부한다. |
| 손상된 상태 파일 | JSON 객체 여부, 필수 항목·자료형·경로·간격을 검사하고 안내 메시지로 종료한다. |
| 동일 백업 반복 저장 | 기록 변경 전의 정상 백업은 처음 한 번만 보존한다. 최신 텍스트 갱신은 유지한다. |
| 구버전 캡처 임시 파일 충돌 | 기존 임시 파일은 보존하고, 이번 호출에서 생성에 성공한 임시 파일만 실패 시 정리한다. |

캡처 목록의 필드·줄 정보·파일 경로 검사와 고유한 백업 임시 파일도 보완했다.
손상된 데이터와 경계값에 대한 검사도 포함한다.

## 자동 테스트

- 71개 모두 통과. 예상 실패 처리나 생략된 테스트 없음.
- Python 구문 검사 통과.
- PNG/PDF의 줄 순서·픽셀·페이지 분할, 기존 파일 보존, 파일명 충돌, 저장 실패·복구 검사 통과.
- 상태 파일 변조, 필수 항목 누락, 자료형 오류, 경로 이탈, 링크 해석, 중복 백업 방지 검사 통과.
- 별도 Windows 정션 실험에서도 기록 폴더 밖을 가리키는 세션을 실제로 거부하는 것을 확인했다.
- 구버전 캡처 복구 테스트도 유지한다.
- 구버전 캡처의 기존 임시 파일 보존과 저장·이름 변경 실패 시 소유한 임시 파일만 정리하는 검사 통과.
  기존 파일 삭제 문제는 수정 전 회귀 테스트로 재현했고, 수정 후 통과를 확인했다.
- 개인 메모·생성물의 Git 제외, 공개 소스·문서 포함, Windows 자동 CRLF 설정에서도 Bash의 LF 유지 검사 통과.
- 실제 WSL 테스트 도구의 기본 배포판 사용·명시적 배포판 선택 옵션 검사 통과.
- 터미널 프로필 전달·공백이 있는 이름·기본 프로필 유지·잘못된 이름 거부·기록 보호 명령 유지 검사 통과.

~~~powershell
python -X utf8 -m unittest discover -s tests -v
python -X utf8 -m py_compile linux_recorder.py terminal_history.py panorama_export.py
~~~

## 실제 WSL 및 PDF

2026-09-20 23:34의 실제 WSL 테스트 통과(Ubuntu-26.04).
600줄 출력, 한글·색상·반복 줄·긴 줄, clear/reset/Ctrl+L 보호와 최종 저장을 확인했다.

- 결과: 명령·안내·프롬프트 포함 625줄, 캡처 24조각, PNG 한 장·PDF 한 페이지.
- 이미지 크기: 1080 × 11875. 기록 프로세스 정상 종료 코드 0.
- PDF의 전체 이미지 픽셀·배치 좌표 비교 통과. 시작·연결부·끝부분을 렌더링해 한글과 연속성을 확인했다.
- 바탕화면에 추가된 최종 파일은 PNG·PDF 두 개이며 중간 기록은 별도 세션 폴더에 보존됐다.

이는 시작 안내 문구 확장 전의 결과다. 따라서 위 줄 수와 이미지 크기는 현재 안내 문구의 출력 길이를 뜻하지 않는다.

### 프로필 지정 실행 확인

2026-09-21 `Ubuntu-26.04` 배포판과 같은 이름의 Windows Terminal 프로필을 지정해 실제 테스트 창을 열었다.
Windows UI Automation으로 확장된 시작 안내·화면 지우기 보호 안내·Bash 프롬프트가 표시되는 것을 확인했고,
테스트에서 만든 창만 닫았다. 기존 창이나 실습 파일은 변경하지 않았다.
이번 프로필 변경에서는 PDF·PNG 전체 생성 테스트를 다시 실행하지 않았다.

## 보안 점검 범위

2026-09-20 실행 코드의 [Bandit](https://bandit.readthedocs.io/en/latest/) 검사: 높은/중간 등급 0건, 낮은 등급 5건.
외부 프로세스 사용 3건과 창 활성화·스크롤 복원의 보조 예외 처리 2건이다.
프로세스 인자는 목록으로 전달하며 shell=True는 사용하지 않는다.
실행 파일 검색 경로와 사용자 셸 설정은 신뢰된 로컬 환경이어야 한다.

2026-09-20 [pip-audit](https://github.com/pypa/pip-audit) 조회에서 실행·테스트 의존성 및 활성 전이 의존성
14개에 알려진 취약점이 없었다. 이후 실행용 패키지는 변경하지 않았다.
이 결과는 해당 날짜·설치 버전에 한정되며 보안 인증이나 미발견 취약점에 대한 보증은 아니다.

자체 업로드·암호화·비밀값 자동 마스킹은 없다. 제출 전에 화면에 나온 개인정보를 확인한다.
본인이 만든 기록만 복구하며, 악의적인 로컬 프로세스가 검사와 사용 사이에 파일을 교체하는 공격까지 보장하지 않는다.

## 공개 준비 점검

- 공개 README의 개인 PC 절대 경로를 제거하고 배포판 이름을 확인하는 절차를 추가했다.
- 개인용 실행 메모는 `LOCAL_USAGE.md`로 분리하고 해당 파일만 정확히 Git에서 제외한다.
- 환경 변수 파일, 실습 기록, 이미지·PDF·로그와 임시 파일의 제외 규칙을 보완했다.
- `.gitattributes`로 Bash 설정 및 소스·문서의 LF 줄바꿈을 지정했다.
- 실제 WSL 테스트 도구에 `--distro` 옵션을 추가해 특정 PC의 배포판 이름에 의존하지 않게 했다.
- 공개 대상 파일에서 개인 경로와 일반적인 비밀키 패턴을 확인했다. 이는 전용 비밀정보 탐지 도구의 전체 이력 검사나 보안 인증은 아니다.

제외 규칙과 줄바꿈은 별도 임시 저장소에서 검사했다. 최종 확인 시 실제 프로젝트에도 Git 저장소가 있었으며,
`git check-ignore`로 개인 메모가 제외되고 `git ls-files`에 추적되지 않는 것을 확인했다.
`git add --dry-run`의 공개 후보 목록에는 개인 메모가 포함되지 않았다.
이번 점검에서 프로젝트의 Git 초기화·스테이징·커밋·푸시는 수행하지 않았으며 라이선스는 미지정 상태다.

## 남은 지원 한계

- 스크롤백 한도를 넘어 사라진 화면, 전체 화면 앱의 전 과정, 창 크기 변경.
- 정전·강제 종료·클라우드 동기화 충돌 시의 완전한 복구.
- 다중 모니터·여러 DPI·원격 데스크톱 조합 전체.
- 상태 파일의 진위 인증, 동시 파일 변조 방어, 민감 정보 마스킹.

이 제한은 알려진 지원 범위이며, 자동 테스트 통과만으로 모든 상황의 안전성을 주장하지 않는다.
